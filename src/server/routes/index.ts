
import express from 'express';

const router = express.Router();

// Since we're removing the backend layer and calling the database directly,
// we don't need any server-side routes or controllers

export default router;
